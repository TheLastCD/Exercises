simplelog.py:
	Simple log with filtering
	uses 2 classes:
		one used for enums and holding the heirachy of logging
		the second implements the logging


optimisedLog.py:
	filtering remains and a few python optimisations from some research
	still uses 2 classes for enum and implementation but:
		Enum class use python library enum 
	the time and date is cached
	Added type hints to optimise using it

